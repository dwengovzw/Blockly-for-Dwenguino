# Folder for shared code

In this folder, you add code that is shared by the frontend and the backend. This can be interfaces for models that are shared between frontend and backend. Make sure to only add pure, isomorphic, fronten-safe code. Do not import backend code!