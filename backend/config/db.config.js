const PLATFORMS = {
    github: "github",
    leerId: "leerId",
    beACM: "beACM"
}

const ROLES = {
    student: "student", 
    teacher: "teacher", 
    admin: "admin",
};

const db = {}
db.PLATFORMS = PLATFORMS;
db.ROLES = ROLES;

export default db